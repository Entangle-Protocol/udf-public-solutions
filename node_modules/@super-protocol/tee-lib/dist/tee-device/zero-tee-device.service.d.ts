/// <reference types="node" />
import { ITeeDeviceService, ParseTlsCertificateResult, TeeDeviceServiceParams } from './interfaces';
/**
 * For testing purpose only.
 */
export declare class ZeroTeeDeviceService implements ITeeDeviceService {
    private readonly logger?;
    constructor(params: TeeDeviceServiceParams);
    parseAndValidateTlsCertificate(): Promise<ParseTlsCertificateResult>;
    getUniqKey(salt: Buffer): Promise<Buffer>;
    getDeviceId(): Promise<Buffer>;
    getDataOf(userData: Buffer): Promise<Buffer>;
}
