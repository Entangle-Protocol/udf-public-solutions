import { TcbVerifiedStatus } from '../../enum';
import { Hash } from "../hashes";
import { Linkage } from "../linkages";
import { Encryption } from "../encryptions";
export declare type GetTlbRequest = {
    isRenewKeys: boolean;
};
export declare type TeeLoaderBlock = {
    nodeName: string;
    deviceId: string;
    tlb: string;
    keys: string;
    pubKey: string;
    meta: string;
};
export declare type TeeConfirmationBlock = {
    tcbId: string;
    deviceId: string;
    properties: string;
    benchmark: number;
    quote: string;
    marks: TcbVerifiedStatus[];
    checkingBlocks: string[];
};
export declare type GetTcbRequest = {
    tcbId: string;
    tcbsForVerification: TeeConfirmationBlock[];
};
export declare type TeeRunInfo = {
    solutionHashes: Hash[];
    linkage: Linkage;
    args: string;
    encryption: Encryption;
};
export declare type TeeInputInfo = {
    tri: TeeRunInfo;
    encryptedResource: Encryption;
};
