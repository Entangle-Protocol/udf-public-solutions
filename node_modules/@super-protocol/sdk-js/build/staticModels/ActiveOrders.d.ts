import { BlockchainId } from '../types';
declare class ActiveOrders {
    static get address(): string;
    /**
     * Function returns amount of active orders
     * @returns {Promise<number>}
     */
    static getListOfActiveOrdersSize(): Promise<number>;
    /**
     * Function returns ids of active orders
     * @returns {Promise<BlockchainId[]>}
     */
    static getListOfActiveOrdersRange(begin?: number, end?: number): Promise<BlockchainId[]>;
    /**
     * Function returns ids of active orders by offers
     * @returns {Promise<BlockchainId[]>}
     */
    static getActiveOrdersRangeByOffers(offerIds: BlockchainId[], begin?: number, end?: number): Promise<BlockchainId[]>;
}
export default ActiveOrders;
