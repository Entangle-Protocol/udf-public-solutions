import { ContractEvents } from 'web3';
import { FilterWithExternalId } from '../types';
import abi from '../contracts/abi';
declare class StaticModel {
    private static readonly logger;
    static findItemsById(eventName: keyof ContractEvents<typeof abi>, filter: FilterWithExternalId, fromBlock?: number | string, toBlock?: number | string): Promise<unknown>;
}
export default StaticModel;
