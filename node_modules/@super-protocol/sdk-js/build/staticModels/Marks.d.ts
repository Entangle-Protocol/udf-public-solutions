import { BlockchainId, Mark, ProviderMarksCount } from '../types';
declare class Marks {
    static get address(): string;
    static getProviderMarks(providerId: string): Promise<ProviderMarksCount>;
    static getOrderMark(orderId: BlockchainId): Promise<Mark>;
    static setOrderMark(orderId: BlockchainId, mark: Mark): Promise<void>;
}
export default Marks;
