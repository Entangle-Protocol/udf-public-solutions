/// <reference types="node" />
import { Resource, ResourceType } from '@super-protocol/dto-js';
import { IResourceLoader, ResourceLoaderConfig } from '../../types/ResourceLoader';
import { BaseResourceLoader } from './BaseResourceLoader';
export declare class StorageProviderLoader extends BaseResourceLoader implements IResourceLoader {
    private static DEFAULT_RETRY_COUNT;
    private static DEFAULT_START_OFFSET;
    static type: ResourceType;
    private progressListener?;
    private chunkSize?;
    private concurrency?;
    private startOffset;
    private retry;
    constructor(config?: ResourceLoaderConfig);
    download(resource: Resource): Promise<Buffer>;
    downloadToFile(resource: Resource, downloadPath: string): Promise<void>;
    private getFileStream;
}
