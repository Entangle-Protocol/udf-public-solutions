import { ResourceType } from '@super-protocol/dto-js';
import { IResourceLoader } from '../../types/ResourceLoader';
export declare const getResourceLoader: (resourceType: ResourceType) => {
    new (): IResourceLoader;
};
