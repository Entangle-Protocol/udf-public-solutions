/// <reference types="node" />
/// <reference types="node" />
import { Readable } from 'stream';
import { Resource } from '@super-protocol/dto-js';
import { IResourceLoader } from '../../types/ResourceLoader';
export declare abstract class BaseResourceLoader implements IResourceLoader {
    abstract download(resource: Resource): Promise<Buffer>;
    abstract downloadToFile(resource: Resource, downloadPath: string): Promise<void>;
    protected downloadToBuffer(readableStream: Readable): Promise<Buffer>;
}
